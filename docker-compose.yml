services:
  web:
    build: .
    # Comanda de mai jos face migrarea și apoi pornește serverul
    command: >
      sh -c "python manage.py migrate && 
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1